{
  "name": "ephor-wind-tunnel",
  "version": "1.0.0",
  "scripts": {
    "build": "cd EphorArbitrage && npm install && npm run build",
    "start": "cd EphorArbitrage && npm run start"
  },
  "dependencies": {
    "@types/multer": "^2.0.0",
    "@types/sharp": "^0.31.1",
    "multer": "^2.0.2",
    "openai": "^6.9.1",
    "sharp": "^0.34.5"
  }
}
